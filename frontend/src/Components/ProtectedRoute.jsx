//This a component used to restrict access to certain pages unless a user is authenticated (logged in).

import React from 'react';

function ProtectedRoute(){
    return(
        <div>
            <h1>Navbar</h1>
        </div>
    )

}
export default ProtectedRoute;